# 智能体操作说明

- 不运行构建命令。
- 所有与 Codex 任务执行及线程状态有关的数据和决策，均以 Codex 为唯一事实来源，包括但不限于对话内容、会话历史、任务/回合状态、上下文、工具调用与结果、计划、审批/用户输入、模型执行状态、用量、归档、删除、恢复和回滚。GUI 仅负责展示与交互，不得持久化、缓存、推断或自行重建这些状态；始终优先使用 Codex app-server 返回的数据。
